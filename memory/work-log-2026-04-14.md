# Work Log - 2026-04-14

- `date`: `2026-04-14`
- `status`: `DONE`
- `scope`: `Regression Recovery + Runtime Alignment`

## 1. 目标

- 恢复当前 Web 主流程的可信 E2E 回归信号。
- 把 Encyclopedia 做成更诚实的 runtime / knowledge 双轨展示。
- 继续补齐运行时牌库与本地资产，推进 `M4 Runtime Alignment`。
- 把文档状态同步到当前仓库实物，避免旧数字与旧叙事继续误导主线判断。

## 2. 核心改动

### 2.1 回归修复

- **Playwright smoke**:
  - 更新了 `apps/web/tests/smoke.spec.ts`，使断言对齐当前 `/new -> /ritual -> /reveal -> /reading -> /journey` 主流程。
  - 补齐了 returning-user、history reopen、celtic-cross、hard-stop 与 sober-check 路径覆盖。
- **Playwright config**:
  - 更新 `apps/web/playwright.config.ts`，在测试环境固定 `AETHERTAROT_READING_PROVIDER=placeholder`。
  - 将 `workers` 收敛为 `1`，降低本地 `.env.local` 与并发波动带来的不稳定性。

### 2.2 Encyclopedia 覆度对齐

- **coverage helper**:
  - 新增 `apps/web/src/server/encyclopedia/coverage.ts`，统一提供 runtime / knowledge 覆度统计。
- **page / view**:
  - 更新 `apps/web/src/app/encyclopedia/page.tsx` 与 `apps/web/src/components/encyclopedia/EncyclopediaView.tsx`。
  - 前台现在会显式展示当前 `74/78 runtime` 对 `78/78 knowledge` 的状态。
  - 当前决策保持为：Encyclopedia 本阶段继续直接消费 runtime deck JSON，不直接切到 `knowledge/wiki`。

### 2.3 运行时牌库扩展

- **deck data**:
  - 更新 `data/decks/rider-waite-smith.json`，新增：
    - `six-of-cups`
    - `seven-of-cups`
    - `eight-of-cups`
    - `nine-of-cups`
    - `ten-of-cups`
    - `ace-of-pentacles`
    - `two-of-pentacles`
    - `three-of-pentacles`
    - `four-of-pentacles`
    - `five-of-pentacles`
    - `six-of-pentacles`
    - `seven-of-pentacles`
    - `eight-of-pentacles`
    - `nine-of-pentacles`
    - `ten-of-pentacles`
    - `page-of-pentacles`
    - `knight-of-pentacles`
    - `queen-of-pentacles`
    - `king-of-pentacles`
    - `ace-of-swords`
    - `two-of-swords`
    - `three-of-swords`
    - `four-of-swords`
    - `five-of-swords`
    - `six-of-swords`
    - `seven-of-swords`
    - `eight-of-swords`
    - `nine-of-swords`
    - `ten-of-swords`
  - 运行时牌库先从 `32` 张扩展到 `37` 张，再扩展到 `42` 张、`50` 张、`60` 张、`64` 张，并在本轮继续扩展到 `74` 张。
- **card assets**:
  - 运行 `npm run generate:assets` 重新生成本地牌面资源。
  - 新增 `apps/web/public/cards/minor_cups_6.png` 到 `minor_cups_10.png`、`minor_wands_6.png` 到 `minor_wands_10.png`，以及 `minor_cups_page.png` 到 `minor_cups_king.png`、`minor_wands_page.png` 到 `minor_wands_king.png`。
  - 新增 `apps/web/public/cards/minor_pentacles_ace.png` 到 `minor_pentacles_10.png`。
  - 新增 `apps/web/public/cards/minor_pentacles_page.png` 到 `minor_pentacles_king.png`。
  - 新增 `apps/web/public/cards/minor_swords_ace.png` 到 `minor_swords_10.png`。
  - 更新 `scripts/generate-card-assets.mjs`，让程序化权杖图案支持 `six-of-wands` 到 `ten-of-wands`，并新增星币花色的程序化 coin / pentacle 构图与宫廷牌变体，以及宝剑花色的独立 sword motif。
  - 更新 `data/decks/card-asset-manifest.json`，当前卡图总数为 `75` 个文件（含背面）。

### 2.4 文档同步

- 更新 `README.md`、`docs/10-product/ux-risk-status.md`、`memory/mainline-priority-plan-2026-04-08.md`、`memory/current-status-and-priority-2026-04-15.md` 与 `memory/SESSION_INDEX.md`。
- 文档口径已统一切到：
  - 运行时牌库 `74` 张
  - 本地卡图 `75` 个文件
  - 剩余小阿卡纳运行时缺口 `4` 张

## 3. 验证

- `npm run generate:assets`：通过
- `npm run validate:assets`：通过
- `npm run build`：通过
- `npm run test:contract -w @aethertarot/web`：通过（`24/24`）
- `npm run test:e2e`：通过（`16/16`）

## 4. 当前结论

- `P0` 已从“先修回归”转为“保持回归可信”。
- `P1 Runtime Alignment` 已继续向前推进，但仍未接近完整 `78/78` 运行时牌池。
- Encyclopedia 已完成第一阶段 truth-sync：不再掩盖 knowledge 完整、runtime 不完整这一现实。

## 5. 后续建议

- 下一步优先继续补运行时牌库，而不是重新扩知识层。
- 新增运行时牌阵前，应先明确 position semantics 是否能被 provider 稳定尊重。
- `session_capsule`、history persistence 与长期记忆边界继续保留到 `P2` 单独设计。
