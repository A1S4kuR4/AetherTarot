# Work Log - 2026-04-15

- `date`: `2026-04-15`
- `status`: `DONE`
- `scope`: `Runtime Alignment Closure + Full 78/78 Deck Coverage + First High-Value Runtime Spread`

## 1. 目标

- 补齐最后一组运行时小阿卡纳缺口。
- 让本地资产、deck manifest、文档口径与完整运行时牌池重新对齐。
- 在完成 `78/78` runtime 牌池后，明确下一主线不再是补牌，而是牌阵与长期连续性。
- 上线首个新增高价值运行时牌阵 `four-aspects`，让完整牌池开始转化成更强的运行时体验。

## 2. 核心改动

### 2.1 宝剑花色运行时收口

- 更新 `data/decks/rider-waite-smith.json`，新增：
  - `ace-of-swords` 到 `ten-of-swords`
  - `page-of-swords`
  - `knight-of-swords`
  - `queen-of-swords`
  - `king-of-swords`
- 运行时牌库从 `74` 张推进到 `78` 张，至此完整 Rider-Waite-Smith 运行时牌池已全部接入。

### 2.2 本地卡图与生成器扩展

- 更新 `scripts/generate-card-assets.mjs`：
  - 新增宝剑花色独立 `swordsMotif`
  - 新增宝剑宫廷牌独立构图分支
- 运行 `npm run generate:assets` 后：
  - 新增 `apps/web/public/cards/minor_swords_ace.png` 到 `minor_swords_10.png`
  - 新增 `apps/web/public/cards/minor_swords_page.png` 到 `minor_swords_king.png`
- 更新 `data/decks/card-asset-manifest.json`，当前本地卡图总数为 `79` 个文件（含背面）。

### 2.3 文档与状态同步

- 更新 `README.md`
- 更新 `docs/10-product/ux-risk-status.md`
- 更新 `memory/current-status-and-priority-2026-04-15.md`
- 更新 `memory/mainline-priority-plan-2026-04-08.md`
- 更新 `memory/SESSION_INDEX.md`

文档口径统一切到：

- 运行时牌库 `78/78`
- 本地卡图 `79` 个文件
- Runtime Alignment 的下一缺口不再是牌义卡覆盖，而是：
  - 高价值牌阵上线顺序
  - Encyclopedia 的 runtime / knowledge 消费边界
  - `session_capsule` / history persistence / 长期连续性设计

### 2.4 首个高价值运行时牌阵落地

- 新增 `data/spreads/four-aspects.json`，将“身体 / 情感 / 心智 / 精神”四层语义正式接入运行时 authority。
- 更新 `packages/domain-tarot/src/index.ts`，把运行时牌阵从 `3` 个扩展到 `4` 个。
- 更新 `/new` 的牌阵选择区与 `/reveal` 的四卡布局，使 `four-aspects` 能以独立的前台节奏完成揭示。
- 补充 reading contract fixture 与 Playwright smoke，确保 authority 位置顺序与 `/new -> /ritual -> /reveal -> /reading -> /journey` 全链路都被回归保护。

## 3. 验证

- `npm run generate:assets`：通过
- `npm run validate:assets`：通过
- `npm run build`：通过
- `npm run test:contract -w @aethertarot/web`：通过（`25/25`）
- `npm run test:e2e`：通过（`17/17`）

## 4. 当前结论

- `P1 Runtime Alignment` 的“运行时牌池覆盖”部分已完成，且首个新增高价值运行时牌阵 `four-aspects` 已落地。
- Encyclopedia 前台展示的 runtime / knowledge 覆度现应为 `78/78` vs `78/78`。
- 当前主线不应再停留在“继续补牌”，而应转向：
  - 第二个高价值运行时牌阵的上线顺序
  - Encyclopedia 是否开始接 `knowledge/wiki`
  - 长期连续性与服务端持久化边界

## 5. 后续建议

- 下一步优先选择第 `2` 个高价值牌阵进入 runtime，而不是一次开多个。
- 在首个新增牌阵已落地后，应补一层与 `coverage.ts` 对应的统计测试，避免后续 deck / asset / spread coverage 漂移。
- `P2` 进入前可先写 `session_capsule` 与 history persistence 设计稿，但先不直接开实现。
