# Work Log - 2026-04-17

- `date`: `2026-04-17`
- `status`: `DONE`
- `scope`: `Safety Realism + Continuity Regression + Second High-Value Runtime Spread`

## 1. 目标

- 把 hard-stop 的示例资源替换为真实、可落地的中国大陆危机 / 心理支持入口。
- 补 continuity 安全边界，确保 `prior_session_capsule` 不会把高风险细节重新带回普通解读链路。
- 补一轮更贴近真实使用的语义与 HTTP 回归。
- 上线第二个高价值运行时牌阵 `seven-card`。

## 2. 核心改动

### 2.1 安全真实化

- 更新 `apps/web/src/server/reading/safety.ts`：
  - Tier 1 hard-stop 文案改为真实中国大陆固定资源顺序：
    - `120` 急性医疗风险
    - `110` 现实危险 / 人身威胁
    - `12356` 心理援助热线
  - `referral_links` 改为真实官方网页入口，不改变既有 `string[]` payload shape。

### 2.2 Continuity 边界收紧

- 更新 `apps/web/src/server/reading/graph.ts`：
  - `prior_session_capsule` 在进入 provider 前新增安全净化。
  - 移除 `用户补充` 类原始细节、自伤/他伤、操控、第三方意图猜测、紧急健康等高风险内容。
  - 若净化后只剩噪音，则按 `null` 处理。
- 同步收紧 completed `session_capsule`：
  - 固定为“问题 / 牌阵 / 核心主题 / 延续主轴 / 边界提醒”模板。
  - 不再直带 `followup_answers` 或原始补充文本。
  - 长度与泄露风险都在服务层被收紧。

### 2.3 回归补强

- 更新 `apps/web/src/server/reading/__tests__/graph.contract.spec.ts`
- 更新 `apps/web/src/server/reading/__tests__/semantic-fixtures.spec.ts`
- 更新 `apps/web/tests/reading-contract.spec.ts`
- 更新 `apps/web/tests/smoke.spec.ts`

新增覆盖包括：

- incoming capsule 净化
- hard-stop 不被 prior capsule 绕过
- completed capsule 不带高风险细节
- lite / standard / sober 三条路径的 continuity 行为一致
- HTTP 403 返回真实危机资源
- seven-card authority 顺序、语义主轴与完整 UI 流程

### 2.4 第二个高价值运行时牌阵：Seven Card

- 新增 `data/spreads/seven-card.json`
- 更新 `packages/domain-tarot/src/index.ts`
- 更新 `packages/prompting/src/index.ts`
- 更新 `/new` 的牌阵选择与 `/reveal` 布局

本轮为 `seven-card` 明确补入：

- `4 -> 7` 的答案 / 结果主轴
- `1 -> 2 -> 3` 的时间线解释
- `5 / 6` 的外部气候 vs 主观投射张力

## 3. 文档同步

- 更新 `README.md`
- 更新 `docs/20-domain/reading-contract.md`
- 更新 `docs/30-agent/context-strategy.md`
- 更新 `docs/30-agent/output-schema.md`
- 更新 `docs/40-architecture/architecture.md`
- 更新 `docs/50-safety/safety-principles.md`
- 更新 `docs/60-evals/rubrics.md`
- 更新 `memory/current-status-and-priority-2026-04-15.md`

## 4. 验证

- `npm run test:contract -w @aethertarot/web`：通过（`38/38`）
- `npm run build`：通过
- `npm run test:e2e`：通过（`23/23`）

## 5. 当前结论

- safety boundary 现在不再只是抽象规则，hard-stop 已返回真实危机 / 心理支持入口。
- continuity 已从“能接上”推进到“能接上且不会把高风险细节重新污染正常链路”。
- `seven-card` 已作为第二个新增高价值运行时牌阵完成 authority、prompt、contract 与前台主流程接入。

## 6. 后续建议

- 下一步不要继续同时开多个牌阵，优先观察 `seven-card` 的真实使用与回归稳定性。
- 若后续要做按地区资源路由或 richer referral schema，应单开 ADR，而不是在当前 `string[] referral_links` 上隐式扩展。
- 长期连续性下一阶段应从 `session_capsule` merge / replay 策略继续推进，而不是直接跳到服务端持久化实现。
