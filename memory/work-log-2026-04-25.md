# Work Log - 2026-04-25

- `date`: `2026-04-25`
- `status`: `DONE`
- `scope`: `Medieval Europe cardsV2 runtime asset switch`

## 1. 目标

用户已在 `apps/web/public/cardsV2/` 重新生成中世纪欧洲风格塔罗卡面，并要求将新的 card 资源应用到项目中。

本轮目标是：

- 让运行时正面牌图统一消费 `cardsV2`
- 让抽牌仪式卡背使用新的 `cardsV2/back.png`
- 维护资源 manifest 与校验脚本，保证路径、数量、尺寸与 hash 可回归
- 不删除旧 `apps/web/public/cards/` 资源，降低切换风险

## 2. 资产核对

确认 `cardsV2` 当前共有 `79` 个 PNG：

- `78` 张正面牌
- `1` 张卡背：`back.png`

核对结论：

- Major Arcana 完整
- Wands / Cups / Pentacles / Swords 四个小阿卡纳花色完整
- 此前缺失的 `minor_pentacles_4.png`、`minor_pentacles_7.png`、`minor_swords_king.png` 已补齐
- `minor_pentacles_2_duplicate.png` 已不再存在于当前资源清单

## 3. 核心实现

### 3.1 Runtime deck imageUrl

更新：

- `data/decks/rider-waite-smith.json`

变更：

- 78 张牌的 `imageUrl` 从 `/cards/...` 切换为 `/cardsV2/...`
- 最终核对：
  - deck card count: `78`
  - `/cardsV2/` imageUrl count: `78`
  - old `/cards/` imageUrl count: `0`

运行时消费路径仍保持原有机制：

- `packages/domain-tarot` 读取 `data/decks/rider-waite-smith.json`
- 前端组件继续消费 `card.imageUrl`
- 未新增第二套牌库读取逻辑

### 3.2 Card back

更新：

- `apps/web/src/constants/index.ts`

变更：

- `CARD_BACK_IMAGE` 从 `/cards/back.png` 切换为 `/cardsV2/back.png`

影响范围：

- `/ritual` 抽牌牌堆背面
- 已抽取但未揭示位置的牌背

### 3.3 Manifest

更新：

- `data/decks/card-asset-manifest.json`

变更：

- manifest `version` 升为 `2`
- 记录 `cardsV2` 的 `78` 张正面牌与 `1` 张卡背
- 每个 entry 记录当前 PNG 的实际 `width`、`height` 与 `sha256`
- `sourceKind` 使用 `image-model-medieval-europe`
- `target.dimensionPolicy` 改为 source-native portrait PNG，实际尺寸以 entry 为准

原因：

- 新一批图不是全部严格 `1000x1700`
- 大多数为接近 `1:1.7` 的 portrait PNG
- 直接强行拉伸会破坏生成图细节与比例

### 3.4 Validator

更新：

- `scripts/validate-card-assets.mjs`

变更：

- 运行时资源根目录从 `apps/web/public/cards/` 改为 `apps/web/public/cardsV2/`
- 正面牌 `imageUrl` 必须以 `/cardsV2/` 开头
- 卡背固定校验 `/cardsV2/back.png`
- 允许 `sourceKind = image-model-medieval-europe`
- 校验从固定 `1000x1700` 改为：
  - portrait ratio 接近 `1:1.7`
  - manifest entry 尺寸必须等于 PNG 真实尺寸
  - manifest hash 必须匹配真实文件
  - 资源目录中不能有未引用 PNG

### 3.5 文档同步

更新：

- `docs/70-ops/card-asset-generation.md`

变更：

- 当前 runtime card assets 目录改为 `apps/web/public/cardsV2/`
- 资源规范改为接近 `1:1.7` 的 full-bleed portrait PNG
- manifest 需要记录真实尺寸与 SHA-256
- validator 描述同步为校验 manifest coverage、portrait ratio、source kind、visual review、recorded dimensions 与 SHA-256

## 4. 资产修正

校验时发现：

- `apps/web/public/cardsV2/minor_wands_4.png`
- 原始尺寸：`1023x1537`
- 比例约 `1:1.50`，明显偏离运行时卡框的 `1:1.7`

处理：

- 仅对 `minor_wands_4.png` 做居中 cover 归一化
- 输出为 `962x1635`
- 随后重算 manifest hash 与尺寸

未做：

- 未批量重采样所有卡面
- 未删除旧 `/cards/` 资源
- 未改变前端卡框比例

## 5. 验证

已运行：

- `npm run validate:assets`
  - 通过
  - 输出：`Validated 78 tarot cards plus card back in apps\web\public\cardsV2.`
- `npm run build`
  - 通过
  - Next.js production build、TypeScript、static page generation 均成功

补充核对：

- `data/decks/rider-waite-smith.json` 中 `/cardsV2/` 引用数为 `78`
- `data/decks/card-asset-manifest.json` 中 `/cardsV2/` 引用数为 `79`
- `CARD_BACK_IMAGE = "/cardsV2/back.png"`

## 6. 注意事项

当前策略是保留旧资源目录：

- `apps/web/public/cards/`

原因：

- 工作树中旧 `/cards/` PNG 已存在较多既有改动
- 本轮目标是将新资源接入 runtime，而不是清理旧资源或迁移所有历史资产

后续如要删除旧 `/cards/`，应单独执行一轮：

- 确认没有引用 `/cards/`
- 确认历史数据或缓存不依赖旧路径
- 更新 docs 中关于旧 procedural generator 的说明
- 再删除旧目录或降级为 archive assets

## 7. 后续建议

- 在浏览器中抽一轮 `/ritual -> /reveal -> /reading`，确认新卡背和新正面牌在移动端与桌面端裁切都自然
- 若 `cardsV2` 后续继续重新生成，优先保持所有 PNG 统一为同一 portrait 尺寸，减少单张归一化
- 若要保留 `scripts/generate-card-assets.mjs`，需要决定它是否继续服务旧 procedural `/cards/`，还是迁移为 `cardsV2` manifest helper
