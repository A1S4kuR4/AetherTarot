# Work Log - 2026-04-20

- `date`: `2026-04-20`
- `status`: `DONE`
- `scope`: `UX Evidence Experience + Decision Boundary + Repeated Theme Reminder + E2E Stability`

## 1. 目标

- 先处理 UX 风险收口中的 `R3` 迎合错觉，不提前打开 memory persistence，也不新增运行时牌阵。
- 把 reading 页从“稳定报告”推进到更有证据感的阅读体验。
- 加强重大现实决策与重复主题追问的前台摩擦，但保持 reading API、schema、provider 与 LangGraph 边界不变。
- 恢复并守住 `npm run test:e2e` 全套稳定性，避免文档继续记录过期状态。

## 2. 核心改动

### 2.1 证据感阅读体验

- 更新 `apps/web/src/components/reading/InterpretationView.tsx`：
  - 逐牌展示显式拆成“牌面线索 / 位置语义 / 综合推断”。
  - 综合段落标记为解释整合层，而不是把所有内容都表现成同等来源的结论。
  - 保持现有 `StructuredReading` schema 不变，不新增 API 字段。

### 2.2 重大现实决策前置确认

- 更新 `/new` 的 `RitualInitializer`：
  - 对明显重大现实决策类问题加入前置现实边界确认。
  - 用户需要确认塔罗只用于整理线索，现实信息、专业意见与个人底线优先，才能进入抽牌仪式。
  - 服务端 Tier 2 `sober_check` 保持独立，前置确认不替代后置安全摩擦。

### 2.3 重复主题提醒

- 基于本地 completed history 中最近若干条相同 `question_type` 的记录，在 `/new` 给出非阻断提醒。
- 该提醒只读取 localStorage history：
  - 不注入 `POST /api/reading`
  - 不打开服务端 persistence
  - 不触发 memory merge
  - 不改变 `session_capsule` 语义

### 2.4 E2E 稳定性恢复

- 更新 `apps/web/src/context/ReadingContext.tsx`：
  - 暴露 ReadingProvider 本地 hydration 完成标记，供 Playwright 等待真实客户端状态。
- 更新 `apps/web/tests/smoke.spec.ts`：
  - 新增 `waitForReadingHydration()`。
  - `gotoAppRoute()` 在继续断言前等待 ReadingProvider hydration。
  - `startReading()` 在填入问题和选择牌阵后，逐项断言用户可见状态已经更新。
  - reload 和 protected route 场景也等待 hydration，避免路由与本地状态恢复竞争。
- `completeFollowup()` 已使用 follow-up section 内部 locator，并逐项断言 textarea value 落值。

## 3. 文档同步

- 更新 `docs/10-product/ux-risk-status.md`：
  - 记录 R3 已通过第一版证据分层部分缓解。
  - 记录重大现实决策确认与重复主题提醒已进入前台机制。
  - 将 E2E 状态同步为 `23/23`，不再保留 `12/23` 或更早的 `11/23` 作为当前事实。
- 更新 `docs/30-agent/context-strategy.md`：
  - 明确重复主题提醒属于本地 UX 层，不等同于长期记忆或服务端 persistence。
- 更新 `memory/current-status-and-priority-2026-04-15.md`：
  - 同步当前 P0 已恢复，`npm run test:e2e` 为 `23/23`。
  - 保留下一步优先级：先守住回归与文档真相，再继续 UX 风险收口。

## 4. 验证

- `npm run test:e2e -w @aethertarot/web -- -g "completes a structured reading"`：通过
- `npm run test:e2e -w @aethertarot/web -- -g "keeps the start button disabled"`：通过
- `npm run test:e2e -w @aethertarot/web -- -g "redirects protected pages"`：通过
- `npm run test:e2e`：通过（`23/23`）
- `npm run test:contract -w @aethertarot/web`：通过（`38/38`）
- `npm run build`：通过
- `npm run lint -w @aethertarot/web`：通过（`0` errors，`13` existing warnings）
- `git diff --check`：通过，仅 CRLF 提示

## 5. 提交

- `2cc89a5 Improve reading evidence presentation`
- `e484704 Add pre-ritual decision boundary confirmation`
- `ec6e02d Add repeated theme reminder`
- `f44f5ea Stabilize web smoke hydration waits`

## 6. 当前结论

- `R3` 迎合错觉已从纯文案风险，推进到 reading 页的可见证据分层机制。
- `R6` 决策外包风险已进一步前移到 `/new` 的用户确认动作，但服务端 safety 仍保持最终边界。
- 重复主题追问已拥有第一版本地、非阻断、非记忆化提醒。
- 当前主线不需要立刻打开 memory persistence，也不需要继续新增牌阵；更应该先打磨既有流程的理解机制与回归稳定性。

## 7. 后续建议

- 下一步优先处理 `R2`：让用户更清楚理解“牌阵如何组织随机”，尤其是 `/reveal` 到 `/reading` 的转折。
- 继续观察 `R3` 证据分层是否足够清晰，必要时调整视觉权重，而不是扩展 schema。
- 再推进 `R7`：在安全边界内增强阅读人格的阻力感与张力点，避免每次都落回温和安抚。
- 继续暂缓服务端 memory persistence、长期画像与新增牌阵，直到当前 UX 风险收口更稳。
