# Work Log - 2026-04-20

- `date`: `2026-04-20`
- `status`: `DONE`
- `scope`: `UX Evidence Experience + Organized Randomness + Constructive Tension + E2E Stability`

## 1. 目标

- 先处理 UX 风险收口中的 `R3` 迎合错觉，不提前打开 memory persistence，也不新增运行时牌阵。
- 把 reading 页从“稳定报告”推进到更有证据感的阅读体验。
- 加强重大现实决策与重复主题追问的前台摩擦，但保持 reading API、schema、provider 与 LangGraph 边界不变。
- 继续处理 `R2` 被组织的随机与 `R7` 人格过于安全可预期，不打开 memory persistence，不新增运行时牌阵。
- 恢复并守住 `npm run test:e2e` 全套稳定性，避免文档继续记录过期状态。

## 2. 核心改动

### 2.1 证据感阅读体验

- 更新 `apps/web/src/components/reading/InterpretationView.tsx`：
  - 逐牌展示显式拆成“牌面线索 / 位置语义 / 综合推断”。
  - 综合段落标记为解释整合层，而不是把所有内容都表现成同等来源的结论。
  - 保持现有 `StructuredReading` schema 不变，不新增 API 字段。

### 2.2 重大现实决策前置确认

- 更新 `/new` 的 `RitualInitializer`：
  - 对明显重大现实决策类问题加入前置现实边界确认。
  - 用户需要确认塔罗只用于整理线索，现实信息、专业意见与个人底线优先，才能进入抽牌仪式。
  - 服务端 Tier 2 `sober_check` 保持独立，前置确认不替代后置安全摩擦。

### 2.3 重复主题提醒

- 基于本地 completed history 中最近若干条相同 `question_type` 的记录，在 `/new` 给出非阻断提醒。
- 该提醒只读取 localStorage history：
  - 不注入 `POST /api/reading`
  - 不打开服务端 persistence
  - 不触发 memory merge
  - 不改变 `session_capsule` 语义

### 2.4 E2E 稳定性恢复

- 更新 `apps/web/src/context/ReadingContext.tsx`：
  - 暴露 ReadingProvider 本地 hydration 完成标记，供 Playwright 等待真实客户端状态。
- 更新 `apps/web/tests/smoke.spec.ts`：
  - 新增 `waitForReadingHydration()`。
  - `gotoAppRoute()` 在继续断言前等待 ReadingProvider hydration。
  - `startReading()` 在填入问题和选择牌阵后，逐项断言用户可见状态已经更新。
  - reload 和 protected route 场景也等待 hydration，避免路由与本地状态恢复竞争。
- `completeFollowup()` 已使用 follow-up section 内部 locator，并逐项断言 textarea value 落值。
- 后续 P0 复测发现 follow-up submit helper 对焦点与按钮稳定状态过敏，已改为确认按钮 enabled 后直接触发 DOM click，避免 motion / React 重挂载造成 false negative。

### 2.5 被组织的随机

- 更新 `apps/web/src/components/reveal/RevealView.tsx`：
  - 新增“牌阵如何组织随机”机制区块。
  - 明确随机决定牌面与正逆位，牌阵决定阅读顺序、位置语义与综合路径。
  - 为当前 5 个运行时牌阵提供前台组织模型，不新增牌阵、不修改 spread authority 数据。
- 更新 `apps/web/src/components/reading/InterpretationView.tsx`：
  - 在正式解读前新增“本次牌阵如何组织随机”说明。
  - 说明逐牌顺序来自权威牌阵位置，继续承接“牌面线索 / 位置语义 / 综合推断”的证据层级。
  - 保持 `StructuredReading` schema、history 与 reading API 不变。

### 2.6 建设性阻力

- 更新 `packages/prompting/src/index.ts`：
  - placeholder initial / final reading 稳定保留一个“建设性阻力”观察。
  - LLM prompt contract 同步要求至少保留一个不完全迎合用户期待的观察点。
  - 该观察必须来自牌面、正逆位、位置语义、牌阵张力或现实未验证条件。
  - 明确禁止把阻力感写成确定性预言、第三方读心、专业替代建议或命令式决策。
  - 后续读感校准已让 `relationship / career / self_growth / decision / other` 使用不同阻力表达，避免“建设性阻力”本身变成新模板句。

## 3. 文档同步

- 更新 `docs/10-product/ux-risk-status.md`：
  - 记录 R3 已通过第一版证据分层部分缓解。
  - 记录 R2 已通过 `/reveal` 与 `/reading` 的“牌阵如何组织随机”机制进一步缓解。
  - 记录 R7 已通过 provider / prompt 的建设性阻力规则进入第一版机制化缓解。
  - 记录重大现实决策确认与重复主题提醒已进入前台机制。
  - 将 E2E 状态同步为 `23/23`，不再保留 `12/23` 或更早的 `11/23` 作为当前事实。
- 更新 `docs/30-agent/context-strategy.md`：
  - 明确重复主题提醒属于本地 UX 层，不等同于长期记忆或服务端 persistence。
- 更新 `docs/20-domain/interpretation-framework.md`：
  - 补充“牌阵组织随机”与“建设性阻力不能覆盖安全边界”。
- 更新 `docs/30-agent/output-schema.md`：
  - 明确组织随机与建设性阻力都复用既有展示层 / 既有字段，不新增协议字段。
- 更新 `docs/50-safety/safety-principles.md` 与 `docs/60-evals/rubrics.md`：
  - 增加建设性阻力的安全边界、跨问题类型差异与评测口径。
- 更新 `memory/current-status-and-priority-2026-04-15.md`：
  - 同步当前 P0 已恢复，`npm run test:e2e` 为 `23/23`。
  - 保留下一步优先级：先守住回归与文档真相，再继续 UX 风险收口。

## 4. 验证

- `npm run test:e2e -w @aethertarot/web -- -g "completes a structured reading"`：通过
- `npm run test:e2e -w @aethertarot/web -- -g "completes a structured reading|supports a full celtic-cross reading flow"`：通过
- `npm run test:e2e -w @aethertarot/web -- -g "keeps the start button disabled"`：通过
- `npm run test:e2e -w @aethertarot/web -- -g "redirects protected pages"`：通过
- `npm run test:e2e`：通过（`23/23`）
- `npm run test:contract -w @aethertarot/web -- src/server/reading/__tests__/semantic-fixtures.spec.ts`：通过（`10/10`）
- `npm run test:contract -w @aethertarot/web`：通过（`40/40`）
- `npm run build`：通过
- `npm run lint -w @aethertarot/web`：通过（`0` errors，`13` existing warnings）
- `git diff --check`：通过，仅 CRLF 提示

## 5. 提交

- `2cc89a5 Improve reading evidence presentation`
- `e484704 Add pre-ritual decision boundary confirmation`
- `ec6e02d Add repeated theme reminder`
- `f44f5ea Stabilize web smoke hydration waits`

## 6. 当前结论

- `R3` 迎合错觉已从纯文案风险，推进到 reading 页的可见证据分层机制。
- `R2` 被组织的随机已从短提示推进到 `/reveal -> /reading` 的可见机制说明，但仍不宣称用户能操纵随机。
- `R6` 决策外包风险已进一步前移到 `/new` 的用户确认动作，但服务端 safety 仍保持最终边界。
- `R7` 人格可预期风险已进入第一版机制化缓解：reading 会保留建设性阻力，但不越过 safety。
- 重复主题追问已拥有第一版本地、非阻断、非记忆化提醒。
- 当前主线不需要立刻打开 memory persistence，也不需要继续新增牌阵；更应该先打磨既有流程的理解机制与回归稳定性。

## 7. 后续建议

- 继续观察 `R2` 的机制说明是否真的降低“纯随机程序噪声”感，而不是增加解释负担。
- 继续观察 `R3` 证据分层与 `R7` 建设性阻力是否足够清晰，必要时调整视觉权重和生成语气，而不是扩展 schema。
- 继续暂缓服务端 memory persistence、长期画像与新增牌阵，直到当前 UX 风险收口更稳。

## 8. 文档漂移复核

- 已复核 `docs/10-product/ux-risk-status.md`、`memory/mainline-priority-plan-2026-04-08.md`、`memory/current-status-and-priority-2026-04-15.md` 与 `memory/SESSION_INDEX.md`。
- 已将仍指向“补 R2 机制缺口 / 继续新增运行时牌阵 / 38/38 contract”的当前建议更新为 `2026-04-20` 事实：R2/R3/R7 已进入第一版机制化缓解，下一步是读感校准；contract 当前为 `40/40`，E2E 当前为 `23/23`。
- 历史 work log 中带日期的旧验证数字保留为历史快照，不作为当前状态入口。
